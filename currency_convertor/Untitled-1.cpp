#include <iostream>  // Preprocessor directive that includes the standard input-output stream library

int main() {  // Main function where program execution begins
    std::cout << "Hello, World!";  // Output stream object to print "Hello, World!" to the console
    return 0;  // Return statement to indicate successful program termination
}
